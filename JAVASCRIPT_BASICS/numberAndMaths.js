// const score1=200
// console.log(score1);

// const score=new Number(200)
// console.log(score);

// console.log( typeof(score));
// console.log( typeof(score1));

const balance = new Number(1000000);
// console.log(balance.toFixed(2))
// console.log(balance.toLocaleString('EN-in'))
// console.log(balance.toString().length)

// In JavaScript, the new keyword is used to create an instance of an object that is based on a constructor function or a class. When you use new, it does several things automatically:

// Creates a new empty object.
// Sets the this value inside the constructor function to the newly created object.
// Executes the constructor function, initializing properties or methods on the new object.
// Returns the new object created by the constructor.

// Number is a built-in JavaScript object constructor. Normally, Number is a function, but when you use new with it, it creates an instance of a Number object.
// new Number(1000000) will create a Number object that wraps the value 1000000.

// ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

// console.log(Math.random());
// console.log(Math.ceil(4.5));
// console.log(Math.floor(4.5));
// console.log(Math.round(4.4));
// console.log(Math.abs(-20));
// console.log(Math.max(10,20,30));

// console.log(Math.floor((Math.random()*10)+1));
// console.log(Math.ceil((Math.random()*10)+1));


// let maxVal=20
// let minVal=10

// console.log(Math.floor(Math.random()*(maxVal-minVal+1))+minVal);

// In JavaScript, Math is a built-in object that provides a set of mathematical functions and constants. 
// It is not a constructor function, so you do not need to create an instance of it using the new keyword.
